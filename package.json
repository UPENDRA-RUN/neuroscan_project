{
  "name": "neuroscan-pro",
  "version": "1.0.0",
  "description": "NeuroScan Pro - Advanced Neuroimaging Analytics Platform",
  "main": "app.py",
  "scripts": {
    "predev": "python3 -c \"import streamlit\" 2>/dev/null || (echo 'Installing Python dependencies...' && python3 -m pip install --user -r requirements.txt)",
    "dev": "streamlit run app.py --server.port 3000 --server.address 0.0.0.0 --browser.gatherUsageStats false",
    "start": "streamlit run app.py",
    "setup": "python3 -m pip install --user -r requirements.txt",
    "check": "python3 -c \"import streamlit, nibabel, numpy, matplotlib; print('All dependencies installed!')\""
  },
  "keywords": [
    "neuroimaging",
    "medical",
    "streamlit",
    "brain-scan"
  ],
  "author": "NeuroScan Team",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {}
}
